package socketchess;

/** Identifiers for the two players in chess. also a special <code>Side.none</code>. */
public enum Side {
    /** Something associated with neither <code>Side.black</code> nor <code>Side.white</code>. */
    none,
    /** Black moves second. */
    black,
    /** White moves first. */
    white 
}
